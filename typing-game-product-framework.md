# 打字训练游戏产品框架 v0.1

## 1. 产品定位

这是一个关卡化、游戏化的盲打训练产品，核心目标是帮助已经形成错误键位肌肉记忆的人，逐步重建正确手位、正确手指路径、盲打节奏和日常输入能力。

它不是普通的测速打字器，也不应该被单一的“归位训练”限制住。更准确的定位是：

> 用游戏关卡把错误肌肉记忆重塑成正确盲打能力。

第一关可以叫“归位”，但产品主线可以继续扩展到出击、巡航、跃迁、节奏、拼音、实战等更多能力。

## 2. 核心用户和当前训练目标

### 当前核心用户

已经会打字，但手位长期放错，形成了稳定但错误的肌肉记忆。具体表现是：

- 手整体放在错误位置，比如比标准盲打基准位偏左一格。
- 不知道标准盲打的手指分工。
- 打字速度慢，但不是因为完全陌生键盘，而是因为原有习惯和正确键位体系冲突。
- 需要重新建立 `A S D F / J K L ;` 的基准位。
- 需要把“看键盘找键”逐渐替换成“从基准位出发的盲打动作”。

### 当前训练目标

第一阶段不追求高 WPM，而是追求：

- 手位正确。
- 每根手指知道自己的“家”和移动范围。
- 打错时能意识到是手位体系错误，而不是单个字母没记住。
- 每次按键后能回到基准位。
- 正确肌肉记忆开始压过旧肌肉记忆。

## 3. 产品原则

### 3.1 先稳定，再加速

早期不奖励盲目快。速度是后期解锁项，第一阶段主要奖励稳定、准确、回位。

### 3.2 游戏刺激感来自“压力”和“反馈”，不是来自惩罚

可以有倒计时、连击、能量槽、轨道偏移、关卡评级，但不使用羞辱式反馈。错误提示要明确、短促、有方向：

- `回到 F/J`
- `右手食指从 J 出发`
- `手位偏左，重新锁定`

### 3.3 每关训练一个明确能力

不要在同一关里同时训练所有东西。每一关都应该有清晰的能力目标：

- 找到基准位。
- 从基准位出击。
- 中排巡航。
- 上下排移动。
- 节奏稳定。
- 拼音输入。
- 真实场景输入。

### 3.4 关卡可扩展

产品底层不写死为“归位工具”，而是用统一关卡模型扩展。

```text
关卡 = 训练目标 + 键位范围 + 出题规则 + 判定规则 + 反馈方式 + 通关条件 + 解锁条件
```

## 4. 产品结构

### 4.1 主界面

第一版可以极简，但要有游戏感：

- 当前章节地图。
- 当前关卡入口。
- 今日训练入口。
- 最近一次成绩。
- 旧习惯复发指标。

第一版不需要完整地图，可以先做一个“任务终端”式界面：

```text
今日任务
01 归位
02 出击
```

### 4.2 游戏内界面

一局训练由五个区域组成：

1. **目标区**  
   显示当前要按的键、组合或短序列。

2. **节奏区**  
   用轨道、光点、脉冲或倒计时制造紧张感。

3. **键盘区**  
   显示局部键盘，强调手指分工和当前目标键。

4. **状态区**  
   显示连击、准确率、稳定度、剩余时间。

5. **反馈区**  
   出错时给短提示，过关时给总结。

### 4.3 结算界面

每局结束后不只显示速度，而是显示训练相关指标：

- 准确率。
- 连续正确最高值。
- 手位稳定度。
- 旧手位复发次数。
- 最容易出错的手指。
- 推荐下一关或重复本关。

## 5. 关卡主线设计

### 第一章：基准建立

目标是让用户理解并接受标准盲打的“出生点”。

关卡：

- 01 归位：锁定 `F/J` 和中排基准位。
- 02 出击：每根手指从家键出发，按目标键，再回家。
- 03 中排巡航：在中排建立连续输入稳定性。

### 第二章：移动路径

目标是建立每根手指到上下排的移动路径。

关卡：

- 04 上排跃迁：练 `QWER / UIOP`。
- 05 下排下潜：练 `ZXCV / NM,.`。
- 06 双排切换：中排和上下排混合。

### 第三章：节奏和抗干扰

目标是让用户在压力下保持正确手位。

关卡：

- 07 节奏轨道：按节拍输入。
- 08 加速巡航：准确率达标后逐步加速。
- 09 干扰清除：插入少量旧手位诱发键，训练抗复发。

### 第四章：拼音实战

目标是连接中文输入场景。

关卡：

- 10 拼音音节：`ni hao`、`wo men`、`jin tian`。
- 11 常用短句：日常聊天和搜索词。
- 12 输入法实战：真实拼音句子节奏。

### 第五章：真实场景

目标是从训练迁移到日常使用。

关卡：

- 搜索任务。
- 聊天回复。
- 邮件短句。
- 命令输入。
- 验证码/账号片段。

## 6. MVP 范围

第一版先做两个可用关卡：

- 01 归位
- 02 出击

第一版目标不是内容很多，而是验证这件事：

> 游戏是否能让用户意识到正确手位，并愿意每天短时间重复练。

### MVP 必须有

- 本地打开即可使用。
- 不需要登录。
- 支持键盘输入捕捉。
- 有实时正确/错误反馈。
- 有局内紧张感。
- 有结算。
- 本地保存最近成绩。

### MVP 可以暂缓

- 用户账号。
- 云同步。
- 复杂地图。
- 多语言。
- 完整 PWA 安装。
- 复杂音效系统。

## 7. 关卡 01：归位

### 7.1 关卡目标

建立正确基准位：

```text
左手：A S D F
右手：J K L ;
```

尤其强化：

```text
左手食指：F
右手食指：J
```

### 7.2 关卡叙事

玩家进入训练舱，必须先让左右手推进器对齐。`F` 和 `J` 是左右锚点，只有锚点锁定后，系统才进入训练。

推荐关卡名：

```text
01 归位
```

可选副标题：

```text
锁定你的左右锚点
```

### 7.3 开局流程

1. 屏幕显示 `F` 和 `J` 两个锚点。
2. 用户需要依次或同时按下 `F`、`J`。
3. 锚点点亮后，进入中排基准键训练。
4. 系统开始出题：`F J F J A ; S L D K` 等。

第一版建议不要强制同时按 `F/J`，因为浏览器键盘事件处理同时按键会增加复杂度。可以先用“依次锁定”：

```text
按 F 锁定左手
按 J 锁定右手
```

### 7.4 输入范围

第一版只训练中排：

```text
A S D F J K L ;
```

为了避免一开始太难，第一阶段权重更高：

```text
F J
```

第二阶段加入：

```text
D K
```

第三阶段加入：

```text
S L
```

第四阶段加入：

```text
A ;
```

### 7.5 出题规则

每局 60 秒或 40 次按键，以先达到者为准。

出题应避免完全随机，采用渐进生成：

```text
第 1 段：F J F J J F
第 2 段：F D J K F J
第 3 段：A S D F J K L ;
第 4 段：F J D K S L A ;
```

推荐第一版使用“当前单键目标”，不要一开始显示长文本。原因是用户当前问题是手位重建，单键反馈更直接。

### 7.6 判定规则

输入正确：

- 当前键点亮。
- 连击 +1。
- 能量槽增加。
- 轨道推进。

输入错误：

- 连击清零或下降。
- 能量槽轻微下降。
- 当前目标保留，不跳过。
- 显示错误类型。

针对旧手位偏左的情况，需要识别“整手左移”倾向。比如目标和实际输入有如下关系时，提示为手位偏移，而不是普通错误：

```text
目标 J，输入 H
目标 K，输入 J
目标 L，输入 K
目标 ;，输入 L
目标 F，输入 D
目标 D，输入 S
目标 S，输入 A
```

提示文案：

```text
手位偏左，回到 F/J
右手锚点在 J
左手锚点在 F
重新归位
```

### 7.7 关卡通关条件

第一版建议通关条件温和一点：

```text
完成 40 次目标输入
准确率 >= 85%
旧手位偏移次数 <= 8
```

三星评级：

```text
1 星：完成训练
2 星：准确率 >= 90%
3 星：准确率 >= 95%，旧手位偏移次数 <= 3
```

### 7.8 局内反馈

正确反馈：

```text
锁定
稳定
很好
继续
```

错误反馈：

```text
回到 F/J
手位偏左
别追速度，先回位
```

尽量短，不要打断节奏。

### 7.9 结算反馈

示例：

```text
本局完成：40 / 40
准确率：88%
归位稳定度：72
旧手位偏移：6 次

建议：重复本关一次，把 F/J 锁定感练稳。
```

## 8. 关卡 02：出击

### 8.1 关卡目标

训练每根手指从基准位出发，按目标键，再回到基准位。

这一关不是让用户记字母位置，而是建立动作路径：

```text
回家 -> 出击 -> 回家
```

### 8.2 关卡叙事

玩家已经完成锚点锁定，现在需要驾驶训练机从基地出发，击中目标后返回基地。每个手指都有自己的航线。

推荐关卡名：

```text
02 出击
```

可选副标题：

```text
从家键出发，再回家
```

### 8.3 输入范围

第一版先训练最关键的两根食指：

左手食指：

```text
F R T G V B
```

右手食指：

```text
J U Y H N M
```

考虑到用户刚开始纠正手位，第一版可以先只做轻量版本：

```text
F J D K R U V M
```

也可以先拆成两个小节：

- 02A 左手食指出击：`F D R V`
- 02B 右手食指出击：`J K U M`

### 8.4 出题规则

出题必须体现“回家”：

```text
F R F
J U J
F V F
J M J
D F D
K J K
```

第一版可以显示为单个目标，但内部使用序列：

```text
F -> R -> F
J -> U -> J
```

用户必须按完整路径，才算完成一个动作。

### 8.5 判定规则

正确路径：

- 家键正确：锚点点亮。
- 目标键正确：命中目标。
- 回家正确：完成一次出击。

如果目标键按对，但没有回家，下一步仍要求回家。

如果用户在回家阶段按错：

```text
先回 F
先回 J
别让手飘走
```

如果用户触发旧手位：

```text
目标 J 却按 H：右手基地在 J，不在 H
目标 F 却按 D：左手基地在 F，不在 D
```

### 8.6 通关条件

建议第一版：

```text
完成 20 次完整出击
路径完整率 >= 80%
准确率 >= 85%
```

三星评级：

```text
1 星：完成 20 次出击
2 星：路径完整率 >= 90%
3 星：路径完整率 >= 95%，旧手位偏移次数 <= 3
```

### 8.7 局内反馈

正确：

```text
出击
命中
回家
航线稳定
```

错误：

```text
先回家
基地在 F/J
手位飘了
重锁锚点
```

### 8.8 结算反馈

示例：

```text
本局出击：20 / 20
路径完整率：84%
准确率：89%
最容易漂移：右手食指

建议：再练一次 J -> U -> J 和 J -> M -> J。
```

## 9. 游戏刺激感设计

### 9.1 连击

连续正确会积累连击。连击越高，视觉节奏越强，但不能让速度失控。

连击阶段：

```text
5 连：稳定
10 连：巡航
20 连：加速
30 连：完美航线
```

### 9.2 能量槽

能量槽奖励准确和稳定：

- 正确 +2。
- 完成完整路径 +5。
- 普通错误 -3。
- 旧手位偏移 -6。

能量槽满时进入短暂“巡航状态”，视觉更爽，但出题难度不突然暴涨。

### 9.3 轨道偏移

当系统识别到旧手位偏移时，画面里的轨道或飞船轻微向左偏，提示用户“手位体系偏了”。重新按 `F/J` 后回正。

### 9.4 倒计时和短局制

每局 45 到 90 秒。短局能带来刺激感，也更适合每天反复练。

第一版建议：

```text
关卡 01：60 秒或 40 次输入
关卡 02：90 秒或 20 次完整出击
```

### 9.5 成就

第一版可以先做轻量成就：

- 首次归位。
- 10 连正确。
- 零旧位复发。
- 第一次三星。
- 连续 3 天训练。

## 10. 风格定义

### 10.1 关键词

```text
街机
训练舱
速度感
清晰反馈
轻赛博
不幼稚
不吵闹
```

### 10.2 视觉方向

推荐风格：

> 夜间训练舱 + 霓虹轨道 + 仪表盘反馈。

画面应有刺激感，但不要变成复杂游戏界面。中心仍然是“目标键”和“手位反馈”。

### 10.3 色彩

建议使用深色底，但不要做成单一蓝紫色。

推荐色彩角色：

- 背景：深墨黑、近黑蓝灰。
- 正确：青绿色。
- 当前目标：暖黄色或电光白。
- 错误：红橙色。
- 旧手位偏移：琥珀橙。
- 完成/能量：亮青或荧光绿。

### 10.4 字体

界面文字可以使用系统无衬线字体，目标键使用等宽字体。

目标键要大、清晰、居中。不要用花哨字体影响识别。

### 10.5 音效

第一版可以先不做复杂音效，但设计上预留：

- 正确：短促清脆。
- 连击：逐渐增强。
- 错误：低频短提示，不刺耳。
- 归位成功：锁定音。
- 过关：简短完成音。

### 10.6 动效

动效应该帮助理解：

- 当前目标键轻微脉冲。
- 正确时目标向前推进。
- 错误时短暂停顿。
- 手位偏移时轨道左偏。
- 重新归位时轨道回正。

## 11. 数据指标

第一版建议记录本地数据：

```text
levelId
playedAt
duration
targets
correctCount
wrongCount
accuracy
maxCombo
homeDriftCount
pathCompleteRate
weakKeys
stars
```

关键不是只记录 WPM，而是记录“纠正肌肉记忆”相关指标。

### 11.1 核心指标

- 准确率。
- 最大连击。
- 旧手位偏移次数。
- 归位稳定度。
- 路径完整率。
- 弱点键位。

### 11.2 归位稳定度计算建议

第一版可以简单计算：

```text
归位稳定度 = 100 - 旧手位偏移次数 * 8 - 普通错误次数 * 3
```

最低为 0，最高为 100。

后续可以改成更精细的算法。

## 12. 关卡配置模型

未来实现时，推荐用配置驱动关卡。

示例：

```json
{
  "id": "level-01-home",
  "title": "归位",
  "subtitle": "锁定你的左右锚点",
  "chapter": "基准建立",
  "durationSeconds": 60,
  "targetCount": 40,
  "keys": ["a", "s", "d", "f", "j", "k", "l", ";"],
  "focusKeys": ["f", "j"],
  "goal": {
    "accuracy": 0.85,
    "maxHomeDrift": 8
  },
  "feedbackMode": "home-position"
}
```

```json
{
  "id": "level-02-strike",
  "title": "出击",
  "subtitle": "从家键出发，再回家",
  "chapter": "基准建立",
  "durationSeconds": 90,
  "targetActions": 20,
  "patterns": [
    ["f", "r", "f"],
    ["j", "u", "j"],
    ["f", "v", "f"],
    ["j", "m", "j"]
  ],
  "goal": {
    "accuracy": 0.85,
    "pathCompleteRate": 0.8
  },
  "feedbackMode": "strike-and-return"
}
```

## 13. 第一版开发任务拆解

### 13.1 页面和状态

- 创建单页应用。
- 创建主菜单。
- 创建关卡选择。
- 创建游戏运行状态。
- 创建结算状态。

### 13.2 输入系统

- 监听键盘输入。
- 阻止浏览器默认快捷行为中会干扰训练的部分。
- 区分正确、普通错误、旧手位偏移。
- 记录每次输入事件。

### 13.3 关卡 01

- 实现 `F/J` 锁定流程。
- 实现中排单键出题。
- 实现准确率、连击、旧手位偏移统计。
- 实现结算。

### 13.4 关卡 02

- 实现路径序列。
- 实现“出击 -> 命中 -> 回家”的阶段提示。
- 实现路径完整率统计。
- 实现结算。

### 13.5 本地存储

- 保存最近成绩。
- 保存每关最好星级。
- 保存最近弱点键。

### 13.6 视觉和反馈

- 训练舱风格界面。
- 目标键高亮。
- 键盘区域高亮。
- 连击和能量槽。
- 错误反馈动效。

## 14. 第一版验收标准

第一版完成时，应该满足：

- 打开页面后可以选择 `01 归位` 和 `02 出击`。
- 每关都能完整开始、训练、结束、结算。
- 键位反馈足够清楚，不需要额外说明也能理解当前该按什么。
- 旧手位偏移有专门提示。
- 结算结果能告诉用户下一步该重复哪类训练。
- 视觉上有游戏感，不像普通表单或纯文字练习器。
- 每局足够短，能让人愿意再来一局。

## 15. 后续扩展方向

### 15.1 内容扩展

- 更多手指路径。
- 中排连续词。
- 上排/下排。
- 拼音音节。
- 中文输入常用词。
- 真实聊天短句。

### 15.2 系统扩展

- 每日训练路线。
- 弱点自动生成关卡。
- 段位和徽章。
- 多用户档案。
- PWA 安装。
- 桌面应用封装。

### 15.3 趣味扩展

- Boss 关：专门对抗某个旧习惯。
- 生存关：准确率维持越久越高分。
- 节奏关：目标键按节拍进入命中区。
- 竞速关：只在基础稳定后解锁。

## 16. 当前推荐产品名方向

不要把名字限制在“归位”，因为产品会扩展。

候选：

- Key Pilot
- Type Shift
- Finger Quest
- 键境
- 盲打航线
- 指尖航线
- 键位重启

当前最贴合游戏感和扩展性的名字：

```text
Key Pilot
```

中文副标题：

```text
盲打训练航线
```

第一关叫：

```text
归位
```

这样产品名可以承载后续更多关卡，而第一关仍然准确表达当前最重要的训练任务。
