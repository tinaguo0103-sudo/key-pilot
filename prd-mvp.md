# Key Pilot MVP PRD v0.1

## 1. 文档目的

本文档定义 `Key Pilot` 下一版 MVP 的产品范围。

它基于：

- `typing-game-product-framework.md`
- `game-design.md`

MVP 的目标不是做完整 RPG，也不是做普通测速打字器，而是验证：

> 地下赛博闯关设定，是否能让“纠正错误键位肌肉记忆”这件事变得更愿意重复练。

## 2. 产品定位

`Key Pilot` 是一款关卡化、游戏化的盲打纠正训练产品。

MVP 包装为：

```text
地下输入网络生存训练
```

玩家身份是“神经驾驶员”，操控 `K-01 重启者` 进入被旧坐标残影污染的地下网络。正确键位让机器人稳定行动；错误键位让机器人偏航、撞墙、护盾受损、怪物靠近。

## 3. MVP 核心目标

### 3.1 用户目标

帮助已经形成错误手位肌肉记忆的人：

- 建立 `F/J` 左右锚点。
- 建立 `A S D F / J K L ;` 中排基准位。
- 训练从家键出击，再回家。
- 明确感知“整手偏左”这种旧坐标复发。
- 愿意每天短时间重复训练。

### 3.2 产品目标

MVP 要验证三件事：

1. 游戏世界观是否增强训练沉浸感。
2. 错误视觉惩罚是否能帮助用户意识到旧手位复发。
3. 两个基础关卡是否足够好玩，能支撑多次重复。

### 3.3 不追求目标

MVP 不追求：

- 高 WPM。
- 完整账号系统。
- 云同步。
- 完整装备背包。
- 完整地图。
- 复杂战斗动画。
- 复杂音效系统。
- 完整 PWA。

## 4. 目标用户

### 4.1 核心用户

已经会打字，但手位长期放错的人。

典型问题：

- 整只手默认位置偏左或偏右。
- 原有打字习惯稳定但错误。
- 不知道标准手指分工。
- 打字速度慢，但不是完全不会打字。
- 需要重新建立标准盲打身体坐标。

### 4.2 MVP 使用场景

- 每天练 5 到 10 分钟。
- 每次玩 1 到 3 局。
- 重点练准确、稳定、回位。
- 用户可以自己感受到“我刚才又偏左了”。

## 5. MVP 世界观落地

### 5.1 世界名称

```text
键境 / The Key Abyss
```

### 5.2 玩家身份

```text
神经驾驶员
```

### 5.3 主角机体

```text
K-01 重启者
```

### 5.4 主要污染

```text
旧坐标残影
```

它代表错误肌肉记忆。MVP 中最重要的残影表现是“手位偏左”。

## 6. MVP 核心玩法循环

1. 用户进入地下任务站。
2. 选择关卡。
3. 进入训练房间。
4. 系统显示当前动作目标。
5. 用户按键。
6. 正确：机器人行动、节点点亮、连击增加。
7. 错误：视觉惩罚、护盾损失、怪物靠近或残影警报。
8. 完成目标后进入结算。
9. 展示训练指标和游戏奖励。
10. 推荐重练或进入下一关。

## 7. MVP 关卡范围

MVP 只做两个核心关卡。

### 7.1 关卡 01：底座校准

原训练名：

```text
归位
```

游戏包装：

```text
底座校准
```

关卡叙事：

K-01 刚进入键境，机体被旧坐标污染。玩家必须先点亮八个中轴稳定台，重新建立身体坐标。

训练目标：

- 锁定 `F/J`。
- 校准 `A S D F / J K L ;`。
- 识别旧手位偏左。

波次设计：

| 波次 | 名称 | 训练内容 | 游戏表现 |
| --- | --- | --- | --- |
| 1 | 开舱锁 | `F/J` | 左右锚点核心点亮 |
| 2 | 内侧底座 | `D/F/J/K` | 内侧稳定台接入 |
| 3 | 外侧底座 | `S/D/F/J/K/L` | 左右稳定台扩展 |
| 4 | 八键同步 | `A S D F / J K L ;` | 中轴全部校准 |

通关条件：

- 完成 48 次目标输入。
- 或时间结束后进入结算。

推荐评级：

- 1 星：完成训练。
- 2 星：准确率 >= 90%。
- 3 星：准确率 >= 95%，旧坐标复发 <= 3。

### 7.2 关卡 02：机械臂清障

原训练名：

```text
出击
```

游戏包装：

```text
机械臂清障
```

关卡叙事：

K-01 进入被小怪和障碍堵住的隧道。玩家必须从基地键伸出机械臂，击中目标后收回。命中不算完成，回家才算完成。

训练目标：

- 训练 `家键 -> 目标键 -> 家键`。
- 强化每根手指自己的出击路径。
- 避免打中后手指漂走。

MVP 建议从当前食指版升级为多手指版：

| 波次 | 手指 | 路径示例 | 游戏表现 |
| --- | --- | --- | --- |
| 1 | 食指 | `F-R-F`, `J-U-J`, `F-V-F`, `J-M-J` | 击碎近距小怪 |
| 2 | 中指 | `D-E-D`, `K-I-K`, `D-C-D`, `K-,-K` | 清除爬行怪 |
| 3 | 无名指 | `S-W-S`, `L-O-L`, `S-X-S`, `L-.-L` | 击退侧墙怪 |
| 4 | 小指 | `A-Q-A`, `;-P-;`, `A-Z-A` | 清理边界怪 |

通关条件：

- 完成 20 次完整路径。
- 或时间结束后进入结算。

评级：

- 1 星：完成训练。
- 2 星：路径完整率 >= 90%。
- 3 星：路径完整率 >= 95%，旧坐标复发 <= 3。

## 8. MVP 怪物范围

MVP 不做复杂战斗，只做 2 到 3 种轻量怪物表现。

### 8.1 偏左残影

对应问题：

```text
整手偏左
```

触发：

- 目标 `J`，输入 `H`。
- 目标 `K`，输入 `J`。
- 目标 `L`，输入 `K`。
- 目标 `;`，输入 `L`。
- 目标 `F`，输入 `D`。
- 目标 `D`，输入 `S`。
- 目标 `S`，输入 `A`。

表现：

- 机器人撞向左墙。
- 画面左倾。
- 隧道轨道偏移。
- 橙红警报。

### 8.2 漂移僵尸

对应问题：

```text
出击后不回家
```

触发：

- 第二关中，回家阶段按错。

表现：

- 机械臂卡住。
- 怪物靠近。
- 返航门关闭。

### 8.3 错位虫群

对应问题：

```text
中排基准位不稳
```

触发：

- 第一关后半段。

表现：

- 多个中排节点出现污染边框。
- 正确按键后逐个净化。

## 9. MVP 惩罚机制

### 9.1 普通错误

效果：

- 当前目标不跳过。
- 连击清零。
- 能量下降。
- 护盾下降。
- 屏幕短暂飘红。
- K-01 打滑或后退半步。

提示示例：

```text
信号打滑了。
目标还在，重新出手。
别急，先稳住底座。
```

### 9.2 旧坐标偏移

效果：

- 比普通错误惩罚更明显。
- 护盾下降更多。
- 污染值上升。
- 轨道左偏。
- 剧情频道吐槽。

提示示例：

```text
旧坐标残影接管了。
你刚才把基地搬家了。
左墙说它不想再被撞了。
```

### 9.3 忘记回家

效果：

- 第二关当前路径不完成。
- 必须回到目标家键。
- 怪物靠近。

提示示例：

```text
命中不算结束，收臂才算。
机械臂还挂在墙上。
先回基地。
```

### 9.4 连续错误

MVP 可做轻量版：

- 连续 2 错：怪物靠近。
- 连续 3 错：护盾裂纹。
- 连续 4 错：建议重锁 `F/J`。

MVP 不强制失败。

## 10. MVP 奖励机制

### 10.1 即时奖励

| 行为 | 奖励 |
| --- | --- |
| 正确输入 | 能量 +2 |
| 完成完整路径 | 能量 +5 |
| 连击 5 | 稳定步伐 |
| 连击 10 | 护盾充能 |
| 连击 20 | 磁轨加速 |
| 零旧坐标复发 | 额外零件 |

### 10.2 结算奖励

结算展示：

- 获得零件。
- 怪物清除数。
- 护盾剩余。
- 图鉴进度。
- 推荐下一步。

MVP 可以只做文案和本地记录，不必实现完整背包。

示例：

```text
获得：锚点芯片碎片 x1
图鉴更新：偏左残影
建议：重复底座校准，把旧坐标复发压到 3 次以内。
```

## 11. MVP 装备范围

MVP 不做完整装备系统，但可以预埋 3 个“待解锁装备”。

| 装备 | 效果 | MVP 表现 |
| --- | --- | --- |
| 锚点心脏 | 偏移提示更明显 | 结算奖励预告 |
| 返航钩爪 | 回家阶段视觉辅助 | 第二关文案预告 |
| 护盾电容 | 抵消一次普通错误 | 后续解锁提示 |

## 12. MVP 页面结构

### 12.1 主界面：地下任务站

必须包含：

- 产品名。
- 世界观一句话。
- 两个关卡入口。
- 最近成绩。
- 最佳准确率/路径完整率。
- 当前机体状态。
- 未来装备/图鉴预告。

### 12.2 关卡内界面

必须包含：

- 当前关卡名。
- 当前波次。
- 当前目标键。
- 当前动作路径。
- K-01 或房间/隧道表现。
- 局部键盘。
- 连击。
- 准确率。
- 护盾/能量/污染至少一种。
- 错误反馈区。
- 剧情频道。

### 12.3 结算界面

必须包含：

- 评级。
- 完成数。
- 准确率。
- 最大连击。
- 旧坐标复发次数。
- 归位稳定度或路径完整率。
- 弱点键位。
- 获得奖励。
- 推荐下一步。

## 13. MVP 数据记录

本地保存即可。

```json
{
  "levelId": "level-01-home",
  "playedAt": "ISO_DATE",
  "duration": 60,
  "targets": 48,
  "correctCount": 45,
  "wrongCount": 3,
  "accuracy": 94,
  "maxCombo": 18,
  "homeDriftCount": 2,
  "pathCompleteRate": 100,
  "stability": 86,
  "weakKeys": ["J", "K"],
  "stars": 2,
  "shieldRemaining": 72,
  "corruption": 18,
  "monstersCleared": 12,
  "partsEarned": ["锚点芯片碎片"]
}
```

## 14. MVP 验收标准

### 14.1 功能验收

- 可以本地运行。
- 可以选择 `01 底座校准` 和 `02 机械臂清障`。
- 两关都有开始、训练、结束、结算。
- 键盘输入捕捉正常。
- 正确、普通错误、旧坐标偏移有不同反馈。
- 第二关必须支持多手指路径，而不只练 `F/J`。
- 本地保存最近成绩。

### 14.2 体验验收

- 第一眼不像普通表单页。
- 关卡内有地下网络/机器人/怪物感。
- 错误时有可感知惩罚。
- 正确时有动作完成感。
- 结算像任务报告，而不是普通成绩单。
- 用户能理解自己是在纠正身体坐标。

### 14.3 训练验收

- 不鼓励盲目提速。
- 目标键清晰。
- 错误后不跳过目标。
- 偏左错误能明确识别。
- 回家阶段必须被强调。

## 15. MVP 版本里程碑

### M1：世界观 UI 改造

- 主界面改为地下任务站。
- 引入 K-01。
- 引入护盾/污染/怪物文案。

### M2：关卡 01 完整游戏化

- 底座校准四波次。
- 八个稳定台可视化。
- 偏左残影反馈。

### M3：关卡 02 多手指清障

- 四波次多手指路径。
- 漂移僵尸或障碍表现。
- 回家失败惩罚。

### M4：结算奖励

- 零件奖励。
- 图鉴更新。
- 推荐下一步。

### M5：打磨

- 移动端适配。
- 文案节奏。
- 错误视觉惩罚。
- 本地数据稳定。

