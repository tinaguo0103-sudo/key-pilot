# Key Pilot 03 巡航防线 PRD v0.7

## 1. 一句话定位

03 巡航防线训练“多手指连续巡航但不丢手位”：玩家在限时压力下连续处理多个键位目标，通过正确手指拦截污染弹、反击远程怪物，并在连续输入中保持 F/J 和中排基准位稳定。

## 2. 为什么不是日常单词或全随机

03 不直接上日常单词。日常单词代入感强，但会引入读词、拼写、词频分布和语言理解干扰。对当前用户问题来说，过早进入真实文本会让旧手位有机会“硬打过去”。

03 也不做纯全键盘随机。纯随机覆盖面好，但缺少游戏语义，容易像考试，而且会让玩家只盯字母反应，不知道每个键应该由哪根手指负责。

03 采用中间态：多手指半随机巡航队列。它覆盖全键盘主要区域，但通过手指比例、左右手比例、锚点回流和限时威胁保持训练意图。

## 3. 与 01/02 的关系

01 战前预检：建立 F/J 主锚和中排八键基准位。

02 机械臂清障：建立单根手指从基地出击再回家的闭环。

03 巡航防线：在连续目标和时间压力下，跨多个手指处理威胁，但不整体偏移、不串指、不丢基准位。

03 不再要求每个目标都显式 `Home -> Target -> Home`，否则会和 02 太像。03 改为“巡航中周期性回流锚点”：每 3 到 5 个目标必须出现一次中排锚点或主锚脉冲，确认手位没有漂走。

## 4. 体验目标

玩家进入一个地下巡航防线房间。K-01 位于中央，周围出现污染弹、远程炮台或怪物投射物。每个污染弹带一个清晰目标键。玩家必须在弹体撞上护盾前按下目标键。按对则拦截或反击；按错或超时则护盾受击、污染上升、怪物压近。

玩家应该明显感到：

- 这不是 02 的回家链重复。
- 连续输入更紧张。
- 手指切换更多。
- 每次目标都清楚。
- 错了有后果，但不是立刻崩盘。
- 越稳定越有节奏感。

## 5. 核心玩法

03 的基本单位是一个 `Threat`，即一个带目标键的威胁。

Threat 包含：

- `key`：当前必须按的目标键。
- `fingerId`：负责手指。
- `side`：左手或右手。
- `row`：上排、中排、下排。
- `spawnLane`：弹体出现位置。
- `deadlineMs`：必须完成的时间窗口。
- `type`：防御、反击、锚点、偏左残影。
- `weight`：队列生成权重。

按对当前键：

- 弹体被护盾拦截或 K-01 反击。
- 连击增加。
- 神经同步链增强。
- 若是锚点目标，手位稳定度恢复。
- 进入下一个 Threat。

按错：

- 当前 Threat 不跳过。
- 护盾轻损。
- 污染上升。
- 弹体更接近或怪物攻击加速。
- 若符合偏左映射，触发旧坐标残影反馈。

超时：

- 等同一次错误。
- 当前 Threat 造成受击后进入下一个 Threat。
- 记录为 `timeoutMiss`，区别于 wrongKey。

## 6. 难度曲线

03 可以明显比 02 难，但需要分阶段递进。

第一段：巡航启动。

- 目标数 8。
- deadline 1800 到 2000ms。
- 主要使用中排和食指/中指。
- 每 3 个目标插入一次 F/J 或当前手指 home。
- 目的：让玩家理解污染弹和倒计时。

第二段：双手交替。

- 目标数 10。
- deadline 1500 到 1800ms。
- 左右手强制交替或半交替。
- 引入上下排目标。
- 目的：训练跨手切换时不丢焦点。

第三段：全指巡航。

- 目标数 12。
- deadline 1300 到 1600ms。
- 覆盖 8 根手指。
- 避免连续 3 个同手同指。
- 目的：建立全键盘区域感。

第四段：污染压迫。

- 目标数 14。
- deadline 1100 到 1500ms。
- 引入突进弹、假动作弹、偏左残影。
- 每 4 个目标插入锚点脉冲。
- 目的：压力下仍保持正确手位。

小 Boss：旧坐标炮台。

- 目标数 16。
- deadline 1000 到 1400ms。
- 混合全指、上下排、锚点回流。
- Boss 发射三种弹：普通污染弹、装甲弹、偏左残影弹。
- 目的：完成 03 的综合验证。

MVP 可先做 3 段共 30 到 36 个 Threat，不必一次做完整 Boss。

## 7. 随机队列规则

03 必须随机，但不是无约束随机。

目标：

- 左右手比例接近 50/50，允许 45/55。
- 每根手指都有出现机会。
- 食指可以略高，因为负责键多，但不能压倒其他手指。
- 上排、中排、下排都出现。
- 不连续大量左手或右手。
- 不连续大量同一根手指。
- 周期性回到中排锚点。

推荐权重：

| 手指 | 目标比例 |
| --- | --- |
| 左小指 | 8% |
| 左无名指 | 10% |
| 左中指 | 12% |
| 左食指 | 20% |
| 右食指 | 20% |
| 右中指 | 12% |
| 右无名指 | 10% |
| 右小指 | 8% |

限制：

- 连续同手最多 2 次，Boss 段最多 3 次。
- 连续同指最多 2 次。
- 连续上排最多 2 次。
- 连续下排最多 2 次。
- 每 3 到 5 个目标必须插入中排锚点或 home-key 目标。
- 偏左残影检测目标优先放在 `J/K/L/;` 和 `F/D/S` 等容易映射的键。

## 8. 键位范围

03 第一版覆盖以下键：

- 左小指：Q A Z
- 左无名指：W S X
- 左中指：E D C
- 左食指：R T F G V B
- 右食指：Y U H J N M
- 右中指：I K ,
- 右无名指：O L .
- 右小指：P ;

第一版可以暂不使用数字行、空格、Shift、退格和符号组合。03 的目标是全字母区巡航，不是完整键盘功能训练。

## 9. 目标显示规则

当前目标键必须在中央偏上或中央区域，字号最大，对比最高。

03 的目标 UI 包含：

- 当前目标键。
- 倒计时环。
- 当前手指通道，例如“左中指”。
- 威胁类型，例如“拦截”或“反击”。
- 下一个目标的小预告，最多 1 个。

不要显示完整键盘大表作为主提示。完整键盘可以作为暂停/教程信息，但训练时不能占据主焦点。

倒计时表达：

- 外圈环逐渐收缩或变色。
- 弹体距离 K-01 越来越近。
- 低于 35% 时间时轻微警示。
- 不能用大面积闪屏。

## 10. 视觉表现

03 房间主题：巡航防线。

空间元素：

- 中央 K-01。
- 周围四向污染弹入口。
- 地面巡航轨道。
- 护盾发生器。
- 墙面远程炮台。
- 顶部或中部目标指令环。
- 边缘低血量和污染层。

敌人方向：

- 污染炮台：固定在墙面，发射字母弹。
- 错位虫群：从轨道缝隙中冒出，干扰巡航节奏。
- 偏左残影弹：弹体有橙红错位拖影，命中后触发左偏反馈。
- 旧坐标炮台：小 Boss，房间地板向左错位并发射混合弹。

K-01 动作：

- 防御成功：护盾前方亮起，对应方向弹体碎裂。
- 反击成功：K-01 发射短能量束击中炮台或怪物。
- 锚点成功：脚下 F/J 或中排基座亮起，手位稳定度恢复。
- 错误：护盾裂纹，K-01 短震。
- 超时：弹体撞上护盾，污染上升。

## 11. 音频表现

03 需要新增 BGM：`cruise_loop_v07.wav`。

音乐方向：

- 比 02 战斗更有稳定脉冲。
- BPM 118 到 124。
- 低频清楚。
- 不用密集高频琶音。
- 危险时通过滤波、音量和少量附加层增强，而不是换成噪声。

新增 SFX：

- `projectile_spawn`
- `projectile_close`
- `shield_block`
- `counter_fire`
- `timeout_hit`
- `wave_clear`

SFX 节奏：

- 每次正确按键都要有清楚反馈，但不能响得像菜单点击。
- 连击增加时可以叠加轻微上扬。
- 超时受击必须和普通错键不同。
- 偏左残影必须沿用独立失真拖拽语言。

## 12. 失败与惩罚

03 引入时间惩罚，但不做新手秒杀。

普通错键：

- 目标不跳过。
- 护盾 -4 到 -6。
- 污染 +2。
- 当前弹体推进 15%。

超时：

- 当前 Threat 结算为 missed。
- 护盾 -6 到 -8。
- 污染 +3。
- 连击断开。

偏左错误：

- 护盾 -6。
- 污染 +6。
- 画面和地板短暂左偏。
- K-01 被拖向左侧。
- 记录 `oldCoordinateRelapse`。

连续错误：

- 怪物攻击频率提高。
- 房间边缘危险层增强。
- 低血量时只闪一下，不持续遮挡目标键。

## 13. 成绩指标

03 结算页新增指标：

- 巡航拦截率：成功处理 Threat 的比例。
- 平均反应窗口：按下时剩余时间比例。
- 手指覆盖率：8 根手指是否都训练到。
- 左右手平衡度：左/右目标比例。
- 锚点回流稳定度：锚点目标正确率。
- 超时受击次数。
- 旧坐标复发次数。
- 最不稳定手指。

任务报告文案示例：

- “巡航拦截率 88%：防线可用，但污染弹仍有突破。”
- “右中指通道不稳定：K/I/, 切换时出现 3 次延迟。”
- “检测到 2 次旧坐标接管：建议回到 01 重锁 F/J 主锚。”
- “锚点回流稳定：多指巡航中基准位未丢失。”

## 14. 资产需求

新增资产目录建议：

- `assets/rooms/cruise/room_cruise_defense_approved_v07.png`
- `assets/monsters/pollution_turret_sheet_approved_v07.png`
- `assets/monsters/misaligned_swarm_sheet_approved_v07.png`
- `assets/vfx/vfx_cruise_projectiles_approved_v07.png`
- `assets/ui/cruise_target_ring_approved_v07.png`
- `assets/audio/bgm/cruise_loop_v07.wav`
- `assets/audio/projectile_spawn.wav`
- `assets/audio/projectile_close.wav`
- `assets/audio/shield_block.wav`
- `assets/audio/counter_fire.wav`
- `assets/audio/timeout_hit.wav`
- `assets/audio/wave_clear.wav`

MVP 可复用 K-01 v04 和部分 02 VFX，但 projectile、目标倒计时环和巡航房间必须新增，否则 03 会像 02 换规则。

## 15. 技术实现建议

新增关卡模式：`cruise`。

建议新增：

- `LEVELS[2]`：03 巡航防线。
- `makeCruiseQueue(level)`：生成受约束随机 Threat 队列。
- `handleCruiseInput(key)`：处理正确、错误、超时。
- `CruiseDefenseScene`：Phaser 主场景。
- `CruiseHud`：中央目标键、倒计时环、下一个目标预告。
- `ThreatActor`：污染弹、炮台弹、残影弹。

可以复用：

- `FINGER_GROUPS`
- `FINGER_GUIDES`
- `DRIFT_LEFT_MAP`
- `AudioDirector`
- `assetManifest`
- 当前桌面版和回归测试链路。

不要复用：

- 02 的 `Home -> Target -> Home` 状态机作为 03 主循环。
- 02 的房间清怪规则作为 03 Threat 结算。
- 右侧完整键盘提示作为 03 主提示。

## 16. 自动化测试要求

03 必须在开发时增加回归测试：

- 队列生成 50 次，左右手比例不超过 45/55 到 55/45。
- 队列生成 50 次，不出现连续 3 个同手或同指。
- 每局至少覆盖 6 根手指，正式目标覆盖 8 根。
- 超时会记录为 missed，且进入下一个 Threat。
- 按错不会跳过当前 Threat，除非弹体已经超时命中。
- 当前目标键始终可见。
- 桌面版和单文件版都能进入 03。

## 17. 验收标准

第一眼验收：03 看起来是新的巡航防线房间，不像 02 清障隧道换标题。

训练验收：连续输入时，玩家确实在多根手指之间切换，而不是一直练 F/J 或食指。

压力验收：玩家能感到限时压力，但仍能看清目标键。

随机验收：两局 03 的目标顺序不同，但左右手和手指比例不离谱。

反馈验收：按对、按错、超时、偏左错误四者有不同表现。

音频验收：03 BGM 有稳定推进感，SFX 清楚但不乱。

复玩验收：玩家完成后愿意再打一局提高拦截率，而不是觉得只是更快的打字测试。

## 18. 首轮开发范围

首轮不做完整剧情、不做装备系统、不做真实单词、不做数字行。

首轮只做：

- 一个 03 巡航防线房间。
- 30 到 36 个 Threat。
- 受约束随机队列。
- 中央目标键 + 倒计时环。
- 污染弹接近。
- 正确拦截、错键、超时、偏左反馈。
- 03 任务报告指标。
- 03 BGM/SFX 初版。
- 桌面版可运行和回归测试。

这足够验证 03 的核心：多手指连续巡航但不丢手位。

