# Key Pilot Game System PRD v0.8

## 目标

Key Pilot 的训练规则必须服务于同一个游戏世界：K-01 在地下键境里通过正确手位重建神经坐标。后续关卡不再把低血量、死亡、怪物动作、音频反馈当成单关临时功能，而是作为所有战斗关卡的基础系统。

## 第一性原则

- 玩家视线焦点永远在主目标键和其附近动作上，关键提示不能放在盲区。
- 训练目标不能被美术遮挡。视觉刺激必须围绕输入判断服务，而不是抢走目标键。
- 失败必须有世界后果：错键、超时、偏左残影都要改变护盾、污染、机体状态或敌人压迫。
- 动作必须读得出来：接入、蓄力、出击、命中、回收、撞盾、死亡都不能只靠文字描述。
- 对抗性验收标准是“不看说明也能知道发生了什么”。

## 全局战斗生命系统

适用范围：02 及之后所有战斗关卡。01 是战前预检，可显示机体指标，但不作为死亡压力关。

- `shield` 是第一层容错。错键、超时、回家错误会优先扣护盾。
- `hull` 是死亡判定。护盾破口后扣机体耐久。
- `pollution` 是旧坐标污染。偏左残影、连续错误、超时会提升污染。
- `low-health` 阈值：耐久低于 40 时，必须出现短促但明显的警报。
- `critical` 阈值：耐久低于 30 时，反馈升级，但不能持续遮挡目标键。
- `death`：耐久归零后结束本局，进入任务报告，报告中明确显示死亡原因和建议复训区域。

## 03 巡航防线 v0.8

训练目标：多手指连续巡航，但不丢手位。03 不再练 02 的三步闭环，而是在时间压力下连续拦截多方向威胁。

### 视觉目标

03 必须像防线战斗，而不是一张图平移：

- 当前威胁应有四段动作：潜伏、蓄力、加速、扑击。
- `projectile`：高速弹体，后段快速冲刺，拦截后爆裂。
- `swarm`：机械虫群，身体抖动和爬行节奏明显，靠近时有扑脸动作。
- `turret`：炮台先锁定充能，后段发射/撞击更重。
- 队列中的后续威胁可以在边缘管线内短暂露出，但不能在屏幕里积攒静止怪。
- 不允许出现调试线条、大圆圈、大面积半透明块、无意义菱形。

### 反馈规则

- 按对：K-01 发出拦截，当前威胁爆裂消失，下一个威胁接管。
- 错键：当前威胁短冲，护盾裂一下，目标不跳过。
- 超时：威胁撞上护盾，护盾损伤和污染残留上升，然后进入下一威胁。
- 连击：K-01 核心和护盾稳定度轻微增强。
- 低血量/死亡：使用全局战斗生命系统。

## 回归验收

每次改 03 必须至少验证：

- 03 场景只有一个 canvas。
- 当前 round 的 actor 数与 round size 一致。
- 可见威胁数量不能大于 3，已清除威胁不能继续可见。
- debug 状态必须标记当前使用 `pounce-v08` 运动系统。
- 强制低血量后仍在游戏中，且有可见警报状态。
- 强制死亡后进入任务报告，reason 为 `death`。

